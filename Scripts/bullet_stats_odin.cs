using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class bullet_stats_odin : NewBullets
{

    // Start is called before the first frame update
 
    //void OnDisable()
    //{
    //    damage *= 10 * FindObjectOfType<gun>().available_spells[0];
    //}
}
